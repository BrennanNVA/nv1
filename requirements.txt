# Core Data Processing
polars>=1.0.0
pydantic>=2.0.0

# Data Sources
alpaca-py>=0.30.0
yahooquery>=2.4.0

# Machine Learning
xgboost>=2.0.0
optuna>=3.0.0
optuna-integration[xgboost]>=0.5.0
scikit-learn>=1.3.0
# mlfinlab>=1.0.0  # Commercial library - using free alternative in src/nova/features/finml_utils.py
shap>=0.45.0  # Model interpretability
statsmodels>=0.14.0  # For ADF test in optimal d selection
# GPU Memory Optimization (optional, for RTX 5070 Ti optimizations)
# RMM is only available via conda, not pip: conda install -c rapidsai -c conda-forge rmm
# rmm>=23.12.0; platform_system != "Windows"  # RAPIDS Memory Manager (Linux/Mac only)

# Sentiment Analysis
ollama>=0.1.0

# Notifications
discord-webhook>=1.0.0

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Dashboard
streamlit>=1.28.0
plotly>=5.17.0

# Configuration
python-dotenv>=1.0.0
pytomlpp>=1.0.0

# MCP Integration
mcp>=0.9.0

# Model Monitoring & Drift Detection
evidently>=0.4.0  # Comprehensive drift detection
nannyml>=0.10.0  # Performance estimation without labels

# Feature Store
feast>=0.38.0  # Feature store infrastructure

# Utilities
# asyncio is part of Python standard library (no need to install)
typing-extensions>=4.8.0
watchdog>=3.0.0

# HTTP Server (for health endpoints)
aiohttp>=3.9.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
